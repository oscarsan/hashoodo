creates sale quotation
confirm sale -> this create an stock operation which state is assigned, products are reserved
stock operation need to be fulfill, row by row, when all rows are done (ordered_qty=qty_done),
 stock operation can be validated, before this, syste, will assign (API) the carrier id,
after that, validate,

TBD

Check if how using the api change the carried id.
Check how using the api validte the stock operation.
